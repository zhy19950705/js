/**
 * 洗牌算法
 */
function shufftle(arr = []) {
  const length = arr.length;
  while (length) {
    let index = (Math.floor(Math.random(length--))[(arr[index], arr[length])] =
      [arr[length], arr[index]]);
  }
}
