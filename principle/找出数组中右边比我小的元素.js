const test = [1, 2, 4, 9, 4, 0, 5]
// 5
function getSmallArr (arr) {
    const length = arr.length
    if (length <= 1) return
    const stack = []
    for (let i = 0; i < length; i++) {

    }
}
function _new(ctor) {
    var obj = Object.create();
    obj.__proto = ctor.property;
    ctor.call(obj);
    
    return obj;

}